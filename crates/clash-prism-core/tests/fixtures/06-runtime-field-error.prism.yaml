# ═══════════════════════════════════════════════════════════
# 测试 Fixture: 运时字段引用（应被编译器拒绝）
# ═══════════════════════════════════════════════════════════
# 这个文件包含运行时字段（delay）在 $filter 中的引用，
# 编译器应该拒绝它并给出清晰的错误信息。

proxies:
  $filter: "p.name.includes('香港') && p.delay < 200"
