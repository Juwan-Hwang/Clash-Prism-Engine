# ═══════════════════════════════════════════════════════════
# 测试 Fixture: $override 冲突（应被编译器拒绝）
# ═══════════════════════════════════════════════════════════
# 这个文件中 $override 与普通键混用，
# 编译器应该拒绝它。

tun:
  $override:
    enable: true
    stack: mixed
  enable: false  # ← 这与 $override 冲突！
